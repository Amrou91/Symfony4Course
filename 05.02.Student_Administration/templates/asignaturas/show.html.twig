{% extends 'base.html.twig' %}

{% block title %}Asignaturas{% endblock %}

{% block body %}
    <h1>Asignaturas</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ asignatura.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ asignatura.nombre }}</td>
            </tr>
            <tr>
                <th>FechaCreacion</th>
                <td>{{ asignatura.fechaCreacion ? asignatura.fechaCreacion|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('asignaturas_index') }}">back to list</a>

    <a href="{{ path('asignaturas_edit', {'id': asignatura.id}) }}">edit</a>

    {{ include('asignaturas/_delete_form.html.twig') }}
{% endblock %}
